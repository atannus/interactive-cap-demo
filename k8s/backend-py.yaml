apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-py-config
  namespace: edu-oe
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: app
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  PORT: "8000"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-py
  namespace: edu-oe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-py
  template:
    metadata:
      labels:
        app: backend-py
    spec:
      containers:
        - name: backend-py
          image: edu-oe/backend-py:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: backend-py-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: backend-py
  namespace: edu-oe
  labels:
    app: backend-py
spec:
  type: LoadBalancer
  selector:
    app: backend-py
  ports:
    - name: http
      port: 8000
      targetPort: 8000
